<h1>Songs</h1>

<ul>
  <%= @songs.each do |song| %>
    <li>
      <%= link_to artist.name, artist_path(artist) %> - <%= link_to song.title, song_path(song) %>
      <%= artist.song_count %> <%= "song.pluralize(artist.song_count)"%>
    </li>
  <% end %>
</ul>
